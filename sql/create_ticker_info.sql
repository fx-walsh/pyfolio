create table lkp.ticker_info (
	sector text,
    num_fulltime_emps integer,
    long_biz_summary text,
    city text,
    state text,
	country text,
    website text,
    ticker varchar(10),
    update_year_month varchar(7)
);